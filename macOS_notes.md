Here are notes on how to setup an macOS machine to compile the binaries.

* Install homebrew
* brew install wget
* brew install gcc
* brew install dos2unix
* brew install pkg-config
* brew install cmake