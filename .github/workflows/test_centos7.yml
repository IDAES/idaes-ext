name: test_centos7

on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: "docker build"
        id: dockerbuild
        uses: eslickj/idaes-ext/docker/test-extensions/centos7@master
        with:
          max_attempts: 1
