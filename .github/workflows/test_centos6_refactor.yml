name: test_centos6_refactor.py

on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: eslickj/idaes-ext-centos6-test:latest
      - name: Install IDAES-PSE
        run: uname -a
